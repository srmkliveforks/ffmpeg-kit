name: srmklive_ffmpeg_kit_flutter_platform_interface
description: A common platform interface for ffmpeg_kit_flutter plugin.
repository: https://github.com/srmkliveforks/ffmpeg-kit
issue_tracker: https://github.com/srmkliveforks/ffmpeg-kit/issues
homepage: https://github.com/srmkliveforks/ffmpeg-kit
# NOTE: We strongly prefer non-breaking changes, even at the expense of a
# less-clean API. See https://flutter.dev/go/platform-interface-breaking-changes
version: 1.0.0

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
